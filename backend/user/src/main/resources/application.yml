spring:
  application:
    name: UserApplication

  datasource:
    url: jdbc:mysql://localhost:3306/users?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update

  config:
    import: optional:configserver:http://localhost:8888
